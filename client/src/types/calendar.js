export const CALENDAR_MODE = Object.freeze({
    YEAR: 'year',
    MONTH: 'month',
    WEEK: 'week'
})

export const WEEK_TYPE = Object.freeze({
    NEXT: 'next',
    PREV: 'previous'
})

export const CALENDAR_TYPE = Object.freeze({
    MAIN: 'main',
    ADDITIONAL: 'additional'
})